# 家庭教育短视频制作工具 - 详细说明文档

## 一、程序概述

本程序是一个基于Python的家庭教育短视频制作工具，集成了MiniMax Token Plan和Qwen Image 2.0等AI服务，实现了从选题生成到视频合成的完整流程自动化。

### 核心功能
- **选题生成**：基于大模型自动生成3个家庭教育选题
- **脚本创作**：根据选题自动生成完整视频脚本（开场钩子+核心干货+CTA引导）
- **分镜设计**：自动生成分镜脚本，包含画面描述和提示词
- **图片生成**：调用AI图像生成API创建视频画面
- **音频制作**：TTS配音和背景音乐生成
- **视频合成**：整合所有素材生成最终视频

### 技术栈
- Python 3.8+
- Tkinter（GUI界面）
- MiniMax Token Plan API（文本、语音、音乐、图像）
- Qwen Image 2.0 API（图像生成）
- Edge-TTS（语音合成）
- FFmpeg（视频合成）
- PIL/Pillow（图像处理）

---

## 二、安装与配置

### 2.1 环境要求
```
Python 3.8+
ffmpeg (需安装并配置到系统PATH)
```

### 2.2 依赖安装
```bash
pip install aiohttp edge-tts Pillow
```

### 2.3 API配置

运行程序后，通过菜单"菜单 > API配置"打开配置窗口：

**MiniMax Token Plan配置**：
- API Key：从MiniMax平台获取的Token Plan Key
- 文本模型：推荐使用 `MiniMax-M2.7`
- 语音模型：推荐使用 `speech-2.8-hd`
- 音乐模型：`Music-2.0`

**Qwen Image 2.0配置**：
- API Key：从阿里云百炼平台获取的API Key

> **注意**：Token Plan Key专用于订阅套餐，不同模型有独立的每日配额。

---

## 三、制作流程（6步）

### 流程概览
```
1. 选题生成 → 2. 脚本创作（含基础字幕）→ 3. 分镜设计（含金句提取）→ 4. 图片生成 → 5. 音频制作（更新时间戳）→ 6. 视频合成
```

### 3.1 Step 1：选题生成
- 点击"生成"按钮
- 自动调用MiniMax API生成3个选题
- 选择一个选题后点击"确认"进入下一步

### 3.2 Step 2：脚本创作
- 自动根据选题生成完整脚本
- 脚本包含：开场钩子、核心干货（3个要点）、CTA引导
- **自动生成基础字幕**：脚本完成后自动进行断句和时间戳预估

### 3.3 Step 3：分镜设计
- 根据脚本自动生成7-10个分镜
- 每个分镜包含：编号、类型、时长、中文描述、英文提示词
- **自动提取金句**：从分镜描述中提取1-2条金句用于重点字幕

### 3.4 Step 4：图片生成
- 调用MiniMax image-01生成图片
- 如果MiniMax生成失败，自动切换到Qwen Image 2.0
- 支持单张图片重新生成

### 3.5 Step 5：音频制作
- **TTS配音**：使用Edge-TTS生成口播音频，支持多种音色和语速
- **背景音乐**：调用MiniMax Music-2.6生成
- **更新时间戳**：根据实际音频时长调整字幕时间戳

### 3.6 Step 6：视频合成
- 整合图片、音频、字幕合成最终视频
- 自动生成封面图片
- 生成交付清单（包含标题方案、发布简介、标签等）

---

## 四、界面功能说明

### 4.1 步骤导航区（左侧）
显示6个步骤的状态：
- **蓝色按钮**：当前步骤
- **绿色按钮**：已完成步骤（可点击跳转）
- **灰色按钮**：未解锁步骤

### 4.2 内容展示区（中间）
根据当前步骤显示不同内容：
- 选题列表（步骤1）
- 脚本编辑（步骤2）
- 分镜表格（步骤3）
- 图片预览（步骤4）
- 音频设置（步骤5）
- 合成结果（步骤6）

### 4.3 操作按钮区（右侧）
- **生成**：生成当前步骤内容
- **确认**：确认当前步骤并进入下一步
- **重新生成**：重新生成当前步骤内容
- **预览**：打开输出文件夹（仅步骤6可用）

### 4.4 菜单功能
- **IP模板设置**：设置IP名称、目标人群、选题范围、内容风格
- **API配置**：配置MiniMax和Qwen API Key
- **存储位置设置**：查看输出文件夹位置

---

## 五、输出文件结构

```
AImove-1/
├── audio/                    # 音频文件
│   ├── voice.mp3             # 口播配音
│   ├── voiceover.mp3         # 备用配音
│   └── bgm.mp3               # 背景音乐
├── images/                   # 生成的图片
│   └── img_*.png             # 分镜图片
├── output/                   # 输出文件
│   ├── final_with_audio.mp4  # 最终视频
│   ├── cover.png             # 封面图片
│   ├── images.txt            # 图片列表（FFmpeg用）
│   └── delivery清单.json     # 交付清单
└── subtitles/                # 字幕文件（预留）
```

---

## 六、交付清单说明

视频合成完成后，自动生成 `output/delivery清单.json`，包含以下内容：

| 字段 | 说明 |
|------|------|
| 选题 | 最终选中的选题标题 |
| 脚本 | 完整脚本内容（opening/content/cta） |
| 分镜数量 | 生成的分镜总数 |
| 分镜摘要 | 各分镜的类型、时长、描述、金句 |
| 字幕示例 | 前5条旁白字幕 |
| 金句 | 提取的金句内容 |
| 标题方案 | 3个备选标题 |
| 发布简介 | 100-150字的发布文案 |
| 标签 | 5个推荐标签 |
| 视频文件 | 视频文件路径 |
| 封面文件 | 封面文件路径 |
| 生成时间 | 生成时间戳 |

---

## 七、常见问题与解决方案

### 7.1 API调用失败
**现象**：生成选题/脚本时显示"使用模拟数据"
**原因**：API Key未配置或配额用尽
**解决方案**：
1. 检查API Key是否正确配置
2. 登录MiniMax/阿里云平台查看配额状态
3. 考虑升级套餐或切换到按量付费

### 7.2 图片生成失败（白板）
**现象**：生成的图片只有提示文字，没有实际画面
**原因**：Prompt格式不正确或图像模型返回空图片
**解决方案**：
1. 检查Prompt是否符合规范
2. 尝试使用Qwen Image 2.0（需配置API Key）

### 7.3 音频生成失败
**现象**：配音文件为空或无法播放
**原因**：Edge-TTS网络问题或语音参数错误
**解决方案**：
1. 检查网络连接
2. 尝试更换音色或调整语速

### 7.4 视频合成失败
**现象**：无法生成最终视频
**原因**：FFmpeg未安装或路径未配置
**解决方案**：
1. 安装FFmpeg并添加到系统PATH
2. 确保所有素材文件存在

---

## 八、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | 2024 | 初始版本，7步流程 |
| v2.0 | 2024-xx-xx | **优化流程**：合并字幕步骤，改为6步流程 |
| | | - 脚本创作时自动生成基础字幕 |
| | | - 分镜设计时提取金句 |
| | | - 音频制作时更新时间戳 |
| | | - 修复字幕解析兼容性问题 |

---

## 九、更新日志

### v2.0 优化内容

1. **流程优化**：从7步简化为6步
   - 取消独立的"字幕生成"步骤
   - 字幕生成整合到脚本创作阶段

2. **字幕处理改进**：
   - 脚本完成后自动生成基础字幕（智能断句）
   - 分镜设计时自动提取金句到字幕
   - 音频生成后根据实际时长调整时间戳

3. **Bug修复**：
   - 修复字幕解析兼容性问题（支持golden字段为列表或字典）
   - 修复`'list' object has no attribute 'get'`错误

4. **文件备份**：
   - 优化前的旧版程序已备份到`历史版/`文件夹
   - Skill文件已备份到`历史版/SKILL_旧版.md`
